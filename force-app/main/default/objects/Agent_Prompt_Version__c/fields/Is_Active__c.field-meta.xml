<?xml version="1.0" encoding="UTF-8" ?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Is_Active__c</fullName>
    <defaultValue>false</defaultValue>
    <description
  >The single version AgentEngine resolves for this agent. Maintained by PromptVersionService.activate; at most one active version per agent is enforced by the unique Active_Key__c field.</description>
    <inlineHelpText
  >Checked on the one version currently used for new runs. Use the Agent Builder to activate a version rather than editing this directly.</inlineHelpText>
    <label>Is Active</label>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>
